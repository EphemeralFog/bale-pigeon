[project]
name = "file-to-bale"
version = "0.1.0"
description = "transfer files using Bale painlessly."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiofiles>=25.1.0",
    "aiohttp>=3.13.5",
]
